# core_modules

Папка для встроенных модулей Max Userbot.

Сейчас встроенные модули объявляются в `userbot.py` (класс `ModuleRegistry.preload_default_modules`).
Следующий шаг — вынести их в отдельные файлы из этой директории.
